# Local gstack session state and security reports — machine-specific, never commit
.gstack/

# CI-generated gitleaks scan report (ci.yml writes this every run) — never commit
gitleaks-report.json

# Local Claude Code permission overrides — machine-specific, never commit
.claude/settings.local.json
.claude/launch.json

# Plugin-scoped skill config (vaultPath overrides for p10/llm-council/the-cabinet) —
# per-user, written by Step 0's first-run AskUserQuestion flow, never commit
/settings.local.json

# Local team config — contains machine-specific paths, never commit
.toto/config.yml

# Node / TypeScript build artifacts
node_modules/
dist/
*.tsbuildinfo

# macOS
.DS_Store

# Local retro snapshots — machine-specific, never commit
.context/

# Local plans and checklists — machine-specific, never commit
plans/
*checklist*
*-checklist.md
